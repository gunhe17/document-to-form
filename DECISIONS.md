# 의사결정 기록 — 대구획 그라운딩 검출 안정화

서식15호 검출 부족(16개, page 모드 57개 대비)을 파고들어 파이프라인을 개선한 과정과 결정.

## 배경
확정 파이프라인은 **대구획 하이라이트 그라운딩**이다: `region_segment → detect_focus_regions(대구획) → 구획별 하이라이트 → LLM 검출 → carve 배치`. 서식15호(사고보고서)만 유독 검출이 부실(16개)해 원인을 추적했다.

## 조사 과정과 기각된 가설
1. **가설 A — "대구획(영역중심) 프롬프트가 검출을 뭉갠다"** → **기각**.
   - 같은 이미지에 프롬프트만 바꾼 A/B 테스트: 결과가 뒤집히고 불안정.
   - page·대구획의 **타입 설명(SYS_V2)은 바이트 단위로 동일**함을 확인. 차이는 user head 2줄뿐.
2. **가설 B — "ground_blocks 후필터(region-clamp)가 드롭"** → **기각**. 계측 결과 53→53 무드롭.
3. **가설 C — "거대 영역 자체"** → 부분적. booklet p11/p12는 거대 구획(98%)인데도 안정(171/129).

## 근본 원인 (확정)
**Stochastic bimodal collapse.** 서식15호는 폼 전체가 한 큰 표라 대구획이 1개 거대 구획(37 atom 중 32개)으로 묶였고, 그 거대 영역 한 콜이 **draw마다 완전열거(~55) ↔ 요약(16)으로 진동**했다.
- 5회 실측: 54·16·56·57·16 (16은 내용까지 동일 재현 = 재현되는 "요약 모드").
- 붕괴는 **셀당 입력이 여럿(□ 7·14개 밀집)** 인 폼에서만. booklet p11(셀=1입력 명단)은 거대해도 안정.
- 저장된 raw 16은 하필 "요약 draw"를 잡은 것.

## 결정

### 1. cap-split — 거대 대구획을 소블록으로 분할 (핵심 수정, 코드)
`ground_focus._split_big_groups(atoms, groups, cap=10)` + `extract_form(max_block=10)`.
거대 영역을 `geo_partition`으로 쪼개니 안정(cap8 실측 54/55/55). 서식15호 16→54.
**프롬프트가 아니라 focus 영역 크기가 지렛대**였다.

### 2. carve 아키텍처 — LLM=검출, carve=위치만 (사용자 확정 원칙)
"애초에 구분된 선택지로 LLM이 검출해야 한다. carve는 검출된 타입의 위치만 일관되게."
- `pipeline._expand_selection`(□ 펼침)·`_split_radio_opt`(옵션 분할) **삭제**.
- cap-split로 LLM이 이미 체크박스를 옵션별 검출(7·7·13·2)하므로 carve 보정 불필요.

### 3. am/pm 분리 — 검출(프롬프트)에서 해결
LLM이 `am/pm`을 어떤 필드는 am·pm 2개, 어떤 필드는 "am/pm" 1덩어리로 들쭉날쭉.
SYS_V2 radio 형태 줄에 **"슬래시 나열 각 낱개가 개별 보기(am/pm→am·pm), 단 쇼크/질식·식사/간식시간 같은 항목명 슬래시는 안 나눔"** 보강.
→ 재그라운딩 시 연락시간·119신고 am/pm이 2개로 분리 + 복합명 보호 확인. 서식15호 54→57.

### 4. 반복 테이블 유지 — 명단 등은 자르지 않음
cap-split이 명단 같은 반복 입력 테이블을 중간에 자르면 헤더·맥락이 끊긴다.
`_is_repeating_table`: 원자가 **≥3열 × ≥3행 규칙 격자**면 반복 테이블 → 통째 유지.
- 서식15호(2열 폼) → 분할(유지). booklet p11(182 atom 명단) → 통째(3→21블록에서 3블록으로).
- 라벨|값 2열 폼·다-□ 밀집은 격자 아님 → 정상 분할.

## 현재 상태
- **서식15호만** 위 개선(cap-split·am/pm) 반영해 재그라운딩됨 (raw 16→57).
- 나머지 17개 raw는 개선 이전 그라운딩. 리포트 `pipeline_raw.html`의 ② 스테이지는 table-aware 소블록을 표시하나, 17개는 ②(소블록)와 ③(옛 그라운딩)이 다른 실행에서 온 상태.

## 남은 일
- **전체 18개 재실행**(~$2)으로 cap-split·am/pm·table-aware를 전 폼에 통일.
- (선택) carve 미세 이슈: 서식8호 "(인)" 위치, 오전/오후 표기 등은 서식 의도에 따른 판단 필요.

## 산출물
- `core/ground_focus.py` — `extract_form`·`_split_big_groups`·`_is_repeating_table`
- `_verify/run_all.py` — 전 서식 실행 → `_verify/raw/*.json`(재사용 raw, LLM 재호출 없이 carve/렌더 재생성)
- `_verify/render_raw.py` → `pipeline_raw.html` — 절차별 고화질 리포트(소블록 반영)
